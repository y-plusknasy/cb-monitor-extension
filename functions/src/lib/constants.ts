/** サーバー側定数 */

/** usageLogs コレクション名 */
export const COLLECTION_USAGE_LOGS = "usageLogs";

/** S01 ではペアリング未実装のため、parentId は固定値 */
export const PARENT_ID_UNLINKED = "unlinked";

/** usageLogs の TTL (日数) */
export const USAGE_LOGS_TTL_DAYS = 30;
