{
  "name": "cb-link-extension",
  "version": "0.2.0",
  "description": "Chrome Extension for CB Link — ブラウザ利用時間トラッカー",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
