{
  "name": "cb-link-extension",
  "version": "1.0.0",
  "description": "Chrome Extension for CB Link — ブラウザ利用時間トラッカー",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
